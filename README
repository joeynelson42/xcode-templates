README 

Run this while in the directory of the Jarvis-iOS-Templates: sudo ./MoveTemplates

If that doesn't work try changing the permissions and run the above command again.

To change permissions run: chmod a+x ./MoveTemplates
